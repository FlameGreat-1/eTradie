apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/part-of: etradie
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/component: edge-trust

commonAnnotations:
  description: "Cloudflare integration: AOP CA bundle + published IP ranges"

resources:
  - configmap-cf-ranges.yaml
  - externalsecret-aop-ca.yaml
