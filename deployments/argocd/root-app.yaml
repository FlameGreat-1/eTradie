# Root "app of apps" Application. Reconciles every YAML under
# deployments/argocd/children/ as its own ArgoCD Application. Adding a
# new environment / service is a single commit to children/ - no
# argocd CLI invocation.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: etradie-root
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: etradie
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: etradie

  source:
    repoURL: https://github.com/FlameGreat-1/eTradie.git
    targetRevision: main
    path: deployments/argocd/children
    directory:
      recurse: true

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=false
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
      # Refuse to prune a child Application without an explicit
      # operator confirmation. Belt + branch-protection. Audit ref:
      # DA-C2.
      - Prune=confirm
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 5m
