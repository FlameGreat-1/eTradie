apiVersion: v1
kind: Service
metadata:
  name: exoper-envoy
  namespace: envoy-system
  labels:
    app: exoper-envoy
    component: proxy
    version: v0.1.0
  annotations:
    description: "Exoper Envoy proxy service - HTTP traffic endpoint"
spec:
  type: ClusterIP
  sessionAffinity: None
  selector:
    app: exoper-envoy
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
    - name: admin
      port: 9901
      targetPort: 9901
      protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: exoper-envoy-headless
  namespace: envoy-system
  labels:
    app: exoper-envoy
    component: proxy
    version: v0.1.0
  annotations:
    description: "Exoper Envoy headless service - for direct pod access"
spec:
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: false
  selector:
    app: exoper-envoy
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
    - name: admin
      port: 9901
      targetPort: 9901
      protocol: TCP
