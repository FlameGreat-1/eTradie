{{- /*
The ACM cert ARN is mandatory in any environment that publishes a
LoadBalancer Service. Fail rendering early rather than producing a
Service that attaches a non-existent certificate to the NLB at apply
time.
*/}}
{{- if eq (.Values.service.type | default "LoadBalancer") "LoadBalancer" }}
{{- if not .Values.service.tlsCertificateArn }}
{{- fail "helm/edge-ingress: .Values.service.tlsCertificateArn is required when service.type=LoadBalancer. Inject the ACM ARN from CI: --set service.tlsCertificateArn=arn:aws:acm:<region>:<acct>:certificate/<id>" -}}
{{- end }}
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "edge-ingress.fullname" . }}
  namespace: {{ include "edge-ingress.namespace" . }}
  labels:
    {{- include "edge-ingress.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.service.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.service.tlsCertificateArn }}
    service.beta.kubernetes.io/aws-load-balancer-ssl-cert: {{ . | quote }}
    service.beta.kubernetes.io/aws-load-balancer-ssl-ports: "tls"
    service.beta.kubernetes.io/aws-load-balancer-ssl-negotiation-policy: "ELBSecurityPolicy-TLS13-1-2-2021-06"
    {{- end }}
    {{- with .Values.service.accessLogS3Bucket }}
    service.beta.kubernetes.io/aws-load-balancer-access-log-enabled: "true"
    service.beta.kubernetes.io/aws-load-balancer-access-log-s3-bucket-name: {{ . | quote }}
    {{- end }}
spec:
  type: {{ .Values.service.type | default "LoadBalancer" }}
  externalTrafficPolicy: {{ .Values.service.externalTrafficPolicy | default "Local" }}
  sessionAffinity: None
  selector:
    {{- include "edge-ingress.selectorLabels" . | nindent 4 }}
  ports:
    - name: tls
      port: {{ .Values.service.ports.tls.port | default 443 }}
      targetPort: tls
      protocol: TCP
      appProtocol: tls
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "edge-ingress.fullname" . }}-metrics
  namespace: {{ include "edge-ingress.namespace" . }}
  labels:
    {{- include "edge-ingress.labels" . | nindent 4 }}
    app.kubernetes.io/component: metrics
spec:
  type: {{ .Values.metricsService.type | default "ClusterIP" }}
  {{- if eq (.Values.metricsService.type | default "ClusterIP") "ClusterIP" }}
  clusterIP: None
  {{- end }}
  selector:
    {{- include "edge-ingress.selectorLabels" . | nindent 4 }}
  ports:
    - name: metrics
      port: {{ .Values.metricsService.port | default 9902 }}
      targetPort: metrics
      protocol: TCP
